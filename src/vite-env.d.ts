/// <reference types="vite/client" />

/** App version injected by Vite's `define` from package.json. */
declare const __APP_VERSION__: string

/** App release channel injected by Vite's `define`. */
declare const __APP_CHANNEL__: string
