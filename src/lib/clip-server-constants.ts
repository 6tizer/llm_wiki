/** Port used by the local clip server. */
export const CLIP_SERVER_PORT = 19827

/** Loopback base URL for the local clip server. */
export const CLIP_SERVER_BASE_URL = `http://127.0.0.1:${CLIP_SERVER_PORT}`
